# AGENTS.md

このリポジトリにツールを追加する AI エージェント／コントリビュータ向けの指針。

## このリポジトリは何か

ブラウザだけで動く小さな web ツールを集めたサイト。GitHub Pages で配信する。
各ツールは `src/<kebab-case-name>/index.html` の単一ファイルで完結する。

## デザインルール（厳守）

- **共通 CSS**: `src/_shared/style.css` を `<link rel="stylesheet" href="../_shared/style.css">` で必ず読み込む。ツール固有 CSS は原則書かない。
- **共通スタイルの拡張**: スタイルが足りないときは個別ツール側で書かず、`src/_shared/style.css` に共通コンポーネントとして追記する。既存ツールを壊さないこと。
- **CSS 変数を使う**: 色や角丸は既存変数 (`--accent`, `--border`, `--card`, `--muted`, `--bg`, `--fg`, `--radius` など) を再利用する。新色をハードコードしない。
- **PC 専用**: スマホ/タブレット対応は考慮不要。メディアクエリで小画面最適化を入れない。
- **外部依存禁止**: CDN、npm パッケージ、外部フォント、外部画像 などは使わない。HTML/JS は単一ファイル内にインライン。
- **言語**: 日本語 UI。`<html lang="ja">` と `<meta charset="UTF-8">` を必ず付ける。

## HTML テンプレート

```html
<!DOCTYPE html>
<html lang="ja">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1">
<title>ツール名 - public-tools</title>
<link rel="stylesheet" href="../_shared/style.css">
</head>
<body>
<div class="container">
  <div class="nav"><a href="../../index.html">← ツール一覧へ戻る</a></div>
  <header class="page-header">
    <h1>ツール名</h1>
    <p class="lead">短い説明（1 文）</p>
  </header>

  <!-- ツール本体 -->

</div>
<script>
  // ツールのロジック
</script>
</body>
</html>
```

## 利用可能な共通クラス

`src/_shared/style.css` で定義済みの主要なクラス。詳細はファイルを参照。

- レイアウト: `.container`, `.nav`, `.page-header`, `.lead`
- 入力: `<input>`, `<textarea>`, `<select>` はタグセレクタで整形済み
- ボタン: `<button>` / `.button`、強調は `.primary`
- アクション行: `.actions`（ボタン横並び）
- カード: `.card`
- 統計表示: `.stats` ＞ `.stat` ＞ `.label` / `.value`（カウンタ系で利用）
- ツール一覧（ルート専用）: `ul.tools` ＞ `li` ＞ `<a>` / `.desc`
- フッター: `footer.page-footer`

## ツール候補（優先度順）

ここから先は **「非エンジニアでも使う一般的なツール」** を優先する（ユーザ指示）。
開発者専用の便利ツール（API テスト、JWT、cron 式など）は低優先。

### 一般向け候補（非エンジニアも使う）

未実装のものを優先的に選ぶ:

- **電卓** （実装済）
- **割り勘計算** （実装済）
- **年齢計算** （実装済）
- **消費税・税込/税抜変換**（税率切替）※電卓に内蔵済だが単体ツールでも可
- **チップ計算**
- **BMI 計算 / 標準体重**
- **生年月日から学年計算**
- **日付計算**（◯日後 / ◯日前 / 期間計算 / 営業日計算）
- **カロリー計算 / 基礎代謝**（年齢・身長・体重・性別から）
- **長さ・重さ・面積・体積・温度の単位換算**（メートル⇄フィート、kg⇄ポンド、℃⇄℉ など）
- **割合計算**（◯% は何 / ◯は何 % など）
- **メモ帳**（自動保存、文字数表示）
- **じゃんけん / コイントス / サイコロ**
- **おみくじ**
- **目覚まし時計 / 世界時計**
- **西暦・和暦・元号変換**（明治以降の元号、西暦 ⇄ 和暦）
- **電気料金計算**（消費電力 W × 時間 × 単価）
- **入学年・卒業年計算**（履歴書用、4 月 1 日基準）
- **TODO リスト / タスク管理**（自動保存、優先度・期限・チェック）
- **ダミー画像生成** （実装済）
- **ストップウォッチ・タイマー** （実装済）
- **ルーレット** （実装済）
- **時間単位変換** （実装済）
- **画像加工** （実装済）
- **QR コード生成** （実装済）

### 生成系（コード/データ生成）候補（ユーザ指示で追加）

ボタンや GUI で簡単にコードやデータを作れるツール。非エンジニアでも値を埋めるだけで使えるよう UI を意識する:

- **Markdown / HTML テーブルジェネレータ** （実装済）
- **CSS Box Shadow ジェネレータ**（X/Y/blur/spread/color をスライダで指定して CSS 出力）
- **CSS Gradient ジェネレータ**（線形/放射状、停止点を視覚編集して CSS 出力）
- **CSS Border Radius ジェネレータ**（4 隅・8 値編集 + プレビュー）
- **CSS Flex / Grid ジェネレータ**（プロパティをスライダ/セレクトで指定して CSS 出力）
- **CSS triangle ジェネレータ**（向き・サイズ・色 → CSS）
- **HTML フォームジェネレータ**（input/select/checkbox を組み立てて HTML 出力）
- **Lorem ipsum 文章ジェネレータ**（段落数 / 文字数指定）
- **ダミーデータジェネレータ**（名前/メール/電話/住所などをランダム生成）
- **OG 画像ジェネレータ**（タイトル+背景色からシェア用 OGP 画像を Canvas 生成）
- **メタタグジェネレータ**（title/description/og:* を入力 → HTML スニペット）
- **.gitignore ジェネレータ**（言語選択 → 対応する .gitignore テンプレ）
- **Crontab ジェネレータ**（曜日/時刻/分などを GUI 指定 → cron 式）
- **配色テーマジェネレータ**（ベース色 → 補色/類似色/トーン違いを生成）

### テキスト操作系候補（ユーザ指示で追加）

非エンジニアも使う「文字を扱うツール」も優先候補:

- **文字置換ツール**（検索文字列・置換文字列の入力、複数同時置換、正規表現切替、大文字小文字無視など）
- **文字検索ツール**（テキスト中のキーワード検索、ヒット位置一覧、ハイライト、件数）
- **文字種変換**（大文字 ⇄ 小文字、全角 ⇄ 半角、ひらがな ⇄ カタカナ、ケバブ/スネーク/キャメル変換）
- **空白・改行整理**（連続空白を 1 つに、改行削除、行頭末空白除去、行ソート、重複行削除）
- **文字並べ替え**（行ソート / 反転 / シャッフル）
- **テキスト統計**（文字数カウンタは実装済。出現頻度の単語ランキングなどは別ツール候補）

これらは正規表現テスター（実装済）と機能が一部重なるが、非エンジニア向けに UI を分けて単機能で使えることを優先する。

### 過去に実装した開発者向け（参考）

差分比較 / コード整形 / カラーピッカー / 正規表現テスター / JSON 整形 / Base64 / URL codec / ハッシュ / UUID / Unix time / パスワード生成 / Markdown / 進数変換 / 文字数カウンタ。

新規実装は上記「一般向け候補」を優先する。

### 実装しないもの（ユーザ指示）

- **JWT デコーダ**: 不要。実装しないこと。
- 開発者専用の用途しか持たない高度なツール（基本は上の「一般向け候補」を優先）。

## 1 イテレーションでやること

1. `ls src/` で既存ツールを確認し、重複しないアイデアを 1 つ決める
2. `src/<kebab-case-name>/index.html` を作成（テンプレート準拠）
3. 必要なら `src/_shared/style.css` に共通コンポーネントを追記
4. ルートの `index.html` のツール一覧 `<ul class="tools">` にリンクと `.desc` を追加
5. HTML 構文と JS の明らかな誤りを目視チェック
6. `git add` で対象ファイルだけステージ → `git commit -m "Add <tool name> tool"`
7. `git push` する（コミットごとに都度 push してよい）
8. 1 イテレーションで **1 ツールだけ** 追加する

## やってはいけないこと

- `.claude/` 配下のファイルをコミットする
- `git push --force` などの破壊的 push
- 既存ツールのスタイルを壊すような共通 CSS の破壊的変更
- 同じツールを別名で重複追加する
- 外部依存（CDN, fonts, npm, fetch 先 API）を持ち込む
- リポジトリ直下の `.nojekyll` を消す（GitHub Pages の Jekyll 処理を無効化している。消すと `_shared/` のような `_` 始まりディレクトリが 404 になる）
