# HTML 閉じタグチェッカ

- カテゴリ: 検証・チェッカー系候補
- 状態: 実装済
- 実装先: `src/html-tag-checker/index.html`

## 概要
HTML の開きタグと閉じタグの対応を検証し、閉じ忘れ・ミスマッチ・余分な閉じタグを行番号つきで表示する。

## 要件メモ
- 自己終了タグ・void 要素を考慮する。
- `script` / `style` の中身は検証対象外として扱う。
